# DO NOT MERGE: intentional zizmor violation to preview CI reporting.
# Only runs on manual dispatch so it never executes automatically.
name: 🚧 Zizmor violation test

on:
  workflow_dispatch:
    inputs:
      name:
        description: "Name"
        required: true

jobs:
  greet:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Hello ${{ github.event.inputs.name }}"
